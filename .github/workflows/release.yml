name: Release

on:
  push:
    branches: [main]
    tags: ['v*']
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
      - '.gitattributes'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - uses: gradle/actions/setup-gradle@v3

      - run: chmod +x gradlew && ./gradlew build

      - name: Tag release — proper release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/libs/OpenWhitelist.jar
          generate_release_notes: true
          draft: false
          prerelease: false

      - name: Push to main — continuous build (pre-release)
        if: github.ref == 'refs/heads/main'
        uses: ncipollo/release-action@v1
        with:
          tag: latest
          name: "Continuous Build"
          artifacts: build/libs/OpenWhitelist.jar
          artifactContentType: application/java-application
          body: "Auto-built from commit ${{ github.sha }}"
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          prerelease: true
          token: ${{ secrets.GITHUB_TOKEN }}
